# App Icon

Place your app icon files here:

- `app_icon.png` — Main app icon (1024x1024 recommended)
- `app_icon_foreground.png` — Android adaptive icon foreground (1024x1024, with padding)

Then run:
```bash
dart run flutter_launcher_icons
```

This will generate icons for all configured platforms (Android, iOS, Windows).
